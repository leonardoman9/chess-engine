# @package _global_
experiment:
  name: cnn_gpu_100k
  description: "CNN large model – ultra-long run (100k games) on GPU"
  total_games: 100000

defaults:
  - override /model: large
  - override /training: production_ultra_long
  - override /exploration: balanced_fast
  - override /agent: large

training:
  eval_frequency: 2000
  eval_games: 60
  log_frequency: 200
  checkpoint_frequency: 2000

agent:
  buffer_size: 200000
  min_buffer_size: 20000

device: "cuda:0"
