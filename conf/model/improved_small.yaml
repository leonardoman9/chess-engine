# @package _global_
model:
  name: improved_small
  conv_channels: [64, 128, 256]  # Più canali per catturare pattern
  hidden_size: 512               # Hidden size maggiore
  dropout: 0.2                   # Dropout maggiore per regolarizzazione
  activation: relu
